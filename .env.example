# === IBM Watson ===
export WATSON_API_KEY="your-watson-api-key"
export WATSON_API_URL="https://api.eu-gb.text-to-speech.watson.cloud.ibm.com/"
export WATSON_REGION="eu-gb"
export WATSON_INSTANCE_ID="your-watson-instance-id"

# === Microsoft Azure ===
export MICROSOFT_TOKEN="your-azure-subscription-key"
export MICROSOFT_REGION="uksouth"

# === Google Cloud TTS ===
# Option 1: Path to credentials file (used in backend only)
export GOOGLE_SA_PATH="./path/to/service-account.json"

# Option 2: Base64-encoded JSON credentials (can be used in CI/CD)
export GOOGLE_SA_FILE_B64="base64-encoded-json-here"

# === AWS Polly ===
export POLLY_REGION="us-east-1"
export POLLY_AWS_KEY_ID="your-aws-access-key-id"
export POLLY_AWS_ACCESS_KEY="your-aws-secret-access-key"

# === ElevenLabs ===
export ELEVENLABS_API_KEY="your-elevenlabs-api-key"

# === Wit.ai ===
export WITAI_TOKEN="your-witai-token"

# === Play.HT ===
export PLAYHT_API_KEY="your-playht-api-key"
export PLAYHT_USER_ID="your-playht-user-id"

# === OpenAI TTS ===
export OPENAI_API_KEY="your-openai-api-key"

# === Sherpa ONNX (Local only) ===
# Sherpa usually doesn’t use env vars — leave blank or define if needed
# export SHERPA_MODEL_PATH="./models/en/encoder.onnx"
# export SHERPA_TOKENS_PATH="./models/en/tokens.txt"